# Deployment trigger

This file intentionally contains no application logic. It exists only to trigger a fresh Vercel deployment from `main` so the existing Vercel routing configuration can be re-evaluated.
